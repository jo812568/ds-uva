=>The project was completed on Ubuntu 16.04 LTS, python 3.5 on jupyter notebook.
=>All parts of the code that contribute to the generation of our results are included in the notebook file
	(populating the scores_tuple struture, run file generation, etc). The TREC-Eval run commands (with
	output exporting to csv) can be found in the report.
=>Run Instructions:
	1)Run jupyter notebook and open our notebook document
	2)Run each block of code from top to bottom
	3)Use the terminal commands provided to generate the Trec EVAL results (and optionally export them to csv)


=>All run files and csv files (and lsi/lda models) have been saved locally and ,if needed, they can be sent along with the assignment
